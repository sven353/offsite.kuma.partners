# offsite.kuma.partners

Full landing page for the Kuma Partners × Nonameyet offsite offering, with an embedded 5-step configurator. Static HTML/CSS/vanilla JS, no build step, no framework.

## Files

- `index.html` — the whole page: header/nav, hero, proof marquee, problem section, formats ("Who will join us"), places ("Three Places to Go"), cadence ("Choose Your Cadence"), the experience gallery cinema filmstrip, engagement models ("Three Ways to Work With Us"), the FAQ accordion, the configurator, the operators section, the discovery-call contact form, footer, plus a hidden static duplicate of the configurator's lead form for Netlify's form detection.
- `partners.html` — new in v19: a standalone page with the full principal-partner roster plus the specialist bench, linked from the "Backed by Kuma's Senior Advisor Network" callout in `#collective` and from nav/footer. As of v21 it also carries the same `#contact` discovery-call form as `index.html`. See "Partners & specialist bench page" and "Discovery call intake form" below.
- `styles.css` — this is the **real, unmodified stylesheet from www.kuma.partners**, copied verbatim (not rewritten). Every class this page uses (`.hero`, `.problem-grid`, `.tier-grid`, `.tier-featured`, `.scorecard-card`, `.scorecard-option`, `.partner-grid`, `.footer-grid`, `.footer-hubs`, button variants, `.field`/`.field-row`/`.select-shell`, `.marquee`/`.marquee-track`, etc.) comes from this file as-is.
- `configurator.css` — supplementary only. It adds the handful of things the real stylesheet doesn't already have: the co-branded logo lockup (real Kuma mark image + hairline divider + "NONAMEYET" text), the photo cards in the places grid, the two-track cinema filmstrip in the experience gallery, the monospace step-numbering prefix inside the configurator, and the blueprint output layout. Nothing in here duplicates or overrides what's already in `styles.css`.
- `app.js` — two things in one file: the same header-scroll-state and mobile-nav-toggle behavior as the live site's `site.js` (reproduced here since it can't be linked cross-origin), and the configurator itself (step data, state, blueprint compilation, Netlify Forms submission). Also exposes `window.selectFormatAndScroll()` (the "Build this format" buttons) and `window.selectCadenceAndScroll()` (the "Select N-Day Cadence" buttons), both called from inline `onclick`, plus click handlers that drive the `#faq` accordion's open/close state and (as of v23) the `#formats` detail drawer's active panel.
- `assets/` — the real Kuma Partners logo lockup and favicon; `assets/formats/` with the three format-card photos (a working session outdoors, an executive strategy session, a large all-hands); `assets/environments/` with the three places photos (Barcelona loft, Catalan Masia, vineyard estate); `assets/gallery/` with the eight experience-gallery filmstrip photos (see "The experience gallery" below for how each slot was actually filled); `assets/team/` with the three principal operator headshots (Sven, Farid, Luisa) plus, as of v29, six specialist bench headshots (Colleen Tartow, Angus Nelson, M&aacute;rcio Marcos, Tiffany Missiha, Daniel Pascual, Bryan Kramer).
- `robots.txt` — crawl rules for search and AI-discovery bots (see "SEO, AI crawler access & structured data" below).
- `sitemap.xml` — two-URL sitemap (`/` and `/partners.html` as of v19; a single URL from v16 through v18, when this was still a one-page site).

## Reused vs. new

Nearly everything on this page reuses classes that already exist in the real site's CSS: `.scorecard-card` / `.scorecard-progress` / `.scorecard-option` power the configurator (it's the same component pattern as the live site's own Leadership Friction Scorecard), `.tier-grid` / `.tier-featured` power both the formats and engagement-models sections, `.problem-grid` / `.problem-col` power the problem section's 3-card row, `.problem-stat` / `.problem-stat-figure` / `.problem-stat-narrative` power the 65% stat lockup at the top of the problem section (this component already existed in the real stylesheet, unused until it was wired in here), `.partner-grid.partner-grid-2col` powers the operators section, `.debrief-form` / `.field` / `.field-row` / `.select-shell` power the lead capture form. `configurator.css` only fills genuine gaps, it never redefines something `styles.css` already handles. The places grid (`.places-grid` / `.place-card`) is the one component built from scratch in `configurator.css`, since nothing in the real stylesheet already does a photo-topped card grid — it follows the same visual language as `.tier-card` (white fill, hairline border, 2px radius, hover lift) rather than inventing a new look. The format cards now reuse that same `.place-image` class for their photos (inline-styled per card for the negative-margin bleed into `.tier-card`'s padding), rather than a separate image treatment.

## The header/footer logo

The header and footer lockup is three real pieces laid out side by side, not a pre-composited image: the actual Kuma Partners logo file (`assets/logo-lockup.png`, pulled from the live site, transparent PNG), a 1px hairline divider, and a plain-text "NONAMEYET" wordmark (Montserrat, underlined). Two of the colors here are deliberate one-offs rather than reuses of `--teal`/`--hairline-color`, called out in the CSS comments: the divider is `#D5D0C7` (slightly warmer than the site's usual hairline so it reads against the frosted header) and the wordmark is `#385E66` (a touch deeper than `--teal`), both matching the reference lockup Sven supplied rather than the closest existing token.

## The hero section

Rebuilt around a new headline and a badge pill that no longer repeats "Barcelona" right before the H1 says it again: the badge now leads with the operating category ("EXECUTIVE ALIGNMENT · DECISION VELOCITY"), the H1 covers what it is and where ("High-Stakes Leadership Offsites in Barcelona"), and the subhead covers who it's for and the concrete deliverable. Under the CTAs sits a geographic capability strip, covered separately below since it changed in v20.

Checked `styles.css` first, as usual: there's a dormant `.north-star-strip`/`.north-star-metric` component that does a label-plus-figure stat pattern, but it's a single centered block, not a multi-column grid with vertical dividers, so it wasn't a real match and wasn't force-fit here. The desktop layout (font sizes, spacing) is set with inline styles directly on the markup, matching how every other section handles one-off sizing on this build (the same pattern as the `.subhead` overrides already used in `#places`, `#problem`, and `#collective`). Only the typeface treatment and the mobile breakpoints are genuinely new CSS, added to `configurator.css` as `.hero-editorial`. The mobile rules use `!important` on every property, which is unusual for this codebase (only one other `!important` exists anywhere in the real stylesheet), but it's load-bearing here rather than a habit: an inline style attribute always beats an external stylesheet rule on specificity, `!important` or not, so without it the mobile breakpoint couldn't override the inline desktop values at all.

## Hero geographic radius strip (v20)

The hero's stat strip used to be four columns: 22@ District, 45 minutes to mountain seclusion, Penedès & Sea, and 5 Languages. The languages metric was redundant with the facilitator section, which already carries a real language display of its own (`.operator-languages-label` / `.lang-pill` in `#collective`), so it came out, and the remaining geography got reorganized into a clearer three-ring story: city center, wine country and coast at roughly 45 minutes, and mountain masia estates at roughly an hour.

Each column now carries an eyebrow tag, a title, and a one-line description, rather than a bare stat pair, so the strip reads as three distinct location profiles instead of four disconnected numbers. This is genuine new markup, not part of the real kuma.partners stylesheet, so it's built as real classes (`.hero-geo-strip`, `.geo-col`, `.geo-tag`, `.geo-title`, `.geo-desc`) in `configurator.css` rather than the inline-style pattern the rest of the hero uses, since it has clean, reusable structure worth naming. The old `.hero-metrics-strip` CSS (base grid plus its two mobile breakpoints) was removed along with the markup it styled. Collapses to a single column on mobile, with the vertical hairline dividers becoming horizontal top borders instead.

## Discovery call intake form (v21)

A new `#contact` section sits at the bottom of both `index.html` and `partners.html`, directly above the footer: "Schedule an Executive Discovery Call", a dark full-bleed section with a copy column (three reassurance bullets, a direct-email fallback) beside a white form card.

This wasn't built from scratch. `styles.css` already carried a complete, real `.contact-section` component (dark navy background, `.contact-grid`, `.contact-copy`, `.contact-reassurance`, `.contact-form-shell`, `.contact-form`, `.contact-success`, all responsive breakpoints included), sitting dormant since the very first design-system audit, mentioned only in passing in a v7 code comment about which real sections use the site's dark full-bleed treatment (alongside `.moat-section`). It had never actually been wired into this build until now. Used as-is rather than the brief's hand-rolled inline-styled section: same field layout, same reassurance-list pattern, and the mobile collapse (`.contact-grid`, `.contact-form .field-row` both drop to one column under 860px) was already written into `styles.css`, so the brief's proposed `.executive-intake-form` mobile breakpoint wasn't needed at all. Zero new CSS this round.

The brief's own form posted to a Formspree endpoint with a placeholder ID (`YOUR_FORM_ID`), which would have shipped a non-functional form. This site already has a working native form-handling path, Netlify Forms, used by the configurator's own lead-capture form, and the project's standing preference is native Netlify Forms over third-party form embeds. Built the same way instead: `<form name="discovery-call" data-netlify="true" netlify-honeypot="bot-field">` with the same visually-hidden honeypot field and hidden `form-name` input already used by the configurator's form. Unlike that form, this one is written directly into the static HTML on both pages rather than rendered by JavaScript, so Netlify's build-time form scan picks it up on its own, no hidden duplicate needed. Netlify treats the two pages' identically-named forms as one, so submissions from either page land in the same place in the dashboard.

Fields: Full Name, Corporate Email, Company & Stage (optional), Estimated Cohort Size (a select using the same four archetype-shaped ranges as the configurator's own Step 1, so the language stays consistent across the site), and Core Mandate or Primary Friction (a textarea). Submission is progressively enhanced in `app.js`: a plain POST to `/` still works with JavaScript off, but with it on, the handler intercepts submit, posts the form data, and swaps the form for a `.contact-success` confirmation inline, matching the confirmation pattern the configurator's own lead form already uses, rather than a full-page reload.

`partners.html`'s bottom CTA row previously sent "Book Discovery Call" to a `mailto:` link, which is what the brief called broken. It now scrolls to `#contact` instead, next to the existing "Configure Your Offsite" button, which still links to `/#configurator` on the homepage.

Verified: tag-balance check on both `index.html` and `partners.html` (clean), `node -c app.js`, a grep confirming `discovery-call` appears exactly once per page and the old `mailto:` "Book Discovery Call" link is gone from `partners.html`, and a jsdom test on both pages that fills the form, submits it, and confirms the form hides and `.contact-success` shows.

## Principal partner cards refreshed, aligned across both pages (v22)

The three principal-partner cards in `#collective` (`index.html`) and the same three on partners.html's "Principal Partners" grid got matching content updates: refined superpower badges and track-record tags, updated bios, and the client-brand badge row (Vistage, Bertelsmann, IESE, and so on) removed from every card on both pages.

Farid's card finally gets a real LinkedIn profile (`linkedin.com/in/faridchaouki`), resolving an unverified placeholder that's been flagged in this build since v8, plus his last name, Chaouki, added throughout. Sven's card gains a third social link, `mulfinger.com`, alongside LinkedIn and email.

Cards with different amounts of content (Sven's five-language block versus Luisa's two, for instance) used to leave each card's footer sitting at a different height depending on how much text was above it. Fixed by making `.partner-card-lg` a column flex container and wrapping the language row and social row in a new `.card-bottom-lock` div (`margin-top:auto`), so the footer always sits flush with the card's bottom edge regardless of content length. Both changes are additive to the real `.partner-card-lg` in `styles.css`, which had no `display` set before, rather than overriding its existing look. The brief's own CSS for this also redefined the card's background, border, and padding to a slightly different treatment; that part wasn't shipped, since the real card's white fill and navy top-border accent are the established look everywhere else the component appears, and only the bottom-locking mechanics were actually needed.

`.partner-photo` also went from 56px to 72px, applied once in `configurator.css` rather than as a repeated inline style on every card, so it reaches every card using that class, including Vincent Az&eacute;'s initials fallback on partners.html, keeping all four cards on that page visually aligned even though the brief's own snippet for partners.html only listed three.

**Vincent Az&eacute; was not in the brief's `partners.html` snippet, and was kept anyway.** The spec's Part 2 HTML for partners.html's `.partner-grid` only included Sven, Farid, and Luisa, the same three cards as the homepage; dropping in that block verbatim would have deleted Vincent's card, added to this page in v19 as a real fourth principal partner. Since nothing in the brief said to remove him and the stated goal was refining and aligning existing content rather than changing the roster, his card was kept, refreshed the same way as the other three (client badges dropped, photo sized to match), just without a `.card-bottom-lock` wrapper, since he has no language row or social row to lock into place.

Luisa's photo path in the brief was given as `assets/team/luisa-abalo.png` again, the same wrong extension caught and corrected back in v19; the real file is still `.jpeg`.

Verified: tag-balance check on both pages (clean), `configurator.css` brace balance (clean), `node -c app.js`, an image-path-existence check across both pages (all resolve), a grep confirming `.client-badges` no longer appears in any of the seven principal/bench-adjacent partner cards on either page, and a jsdom check confirming card counts (3 on index.html, 4 on partners.html), `.card-bottom-lock` presence on the three refreshed cards on both pages, Farid's corrected LinkedIn href, and Sven's three-link social row.

## Calibrated Formats detail drawer (v23)

`#formats` (index.html only) used to pack a dense four-item checkmark list into each of the three tier-cards. Those lists have moved out of the cards entirely: each card now carries a short italic mandate line plus three scannable metric pills, and a single full-width detail drawer sits below the 3-card grid, showing the deep curriculum, facilitation, and deliverable specs for whichever format is currently selected.

Card shell, badge, label, subhead, and image treatment are unchanged, still the real `.tier-grid`/`.tier-card`/`.tier-badge-pill`/`.tier-label`/`.tier-subhead`/`.place-image` system from the v17 refactor. The metric-pill row under each mandate reuses `.client-badges` verbatim for its visual treatment, the same reuse pattern `.immersion-meta` already uses elsewhere in `configurator.css`. The "Inspect full scope" toggle on each card reuses the real `.tier-cta-sub` text button, defined in the real stylesheet for exactly this `.tier-actions` slot but never referenced by any markup until now. The drawer's small eyebrow label reuses the real, equally dormant `.case-docket-badge` from the never-wired case-study docket component, and the two-column spec lists reuse the real, dormant `.outcomes-list` checkmark styling rather than redefining that glyph a third time; `.outcomes-list-1col` is a one-line modifier that narrows its native two-column grid down to one column per labeled group, since each format needs two separately headed lists (not one flat checklist). The drawer shell itself, its header row, and the "currently selected" card highlight are genuinely new, nothing in the real stylesheet does a full-width, swappable detail panel.

The toggle logic in `app.js` mirrors the FAQ accordion's own pattern: one active panel at a time, driven by a class/attribute toggle rather than inline `style.display` swaps. "Build this format" (each card's primary CTA, which pre-selects the archetype and jumps to the configurator) and the new drawer's own "Configure this format" button both still call the existing `selectFormatAndScroll()`, untouched by this round. On mobile, opening a card's detail smooth-scrolls the drawer into view, since it sits below the fold of a 3-card stack at that width.

The C-suite / Strategy-to-Execution format is expanded by default (it already carries the "Most Requested" badge), matching what the earlier version showed most prominently.

Verified: tag-balance check on `index.html` (clean), `configurator.css` brace balance (clean), `node -c app.js`, an image-path-existence check (all resolve, no new images added), and a jsdom test exercising all three toggles, confirming the active card, the visible panel, and each toggle's `aria-expanded` state update correctly, and that `selectFormatAndScroll` and the "Build this format" wiring are unaffected.

## Nav label: "Cohort Architecture" (v24)

The header nav link for `#formats`, on both `index.html` and `partners.html`, changed from "Who Will Join Us" to "Cohort Architecture", to match the section's own eyebrow label and heading ("Cohort Architecture & Mandate" / "Calibrate by Cohort & Mandate").

The brief also asked for a matching footer change, assuming the footer nav mirrors the header nav one-for-one. It doesn't: the footer's "Engagements" column lists the three individual format names (The Slowing-Down Offsite / Strategy-to-Execution Offsite / Company Offsite) plus "Build Your Offsite" and "Partners & Bench", not a copy of "Who Will Join Us". That phrase only exists once per page, in the header `.nav-links` list, so that's the only place changed. Nothing in the footer needed touching.

Verified: tag-balance check on both pages (clean), a grep confirming zero remaining instances of "Who Will Join Us" on either page and exactly one "Cohort Architecture" nav link on each.

## Cadence cards standardized; nav repointed to #cadence (v25)

The three `#cadence` duration cards (2-day / 3-day / 5-day) used to have inconsistent shapes: the 2-day and 5-day cards each carried 4 plain bullet points, while the featured 3-day card carried 3 bullets with bold "Day 1:" / "Day 2:" / "Day 3:" prefixes nothing else on the page uses. None of the three had a one-line mandate under the subhead, unlike `#formats`' cards (which gained one back in v17). All three now share an identical shape: a `.tier-mandate` summary line, exactly 3 plain unprefixed bullets each, and a "Key Monday Deliverable" callout, so the row reads as three parallel offers rather than three differently-structured cards that happen to sit side by side.

Zero new CSS this round. The mandate line reuses the real `.tier-mandate` class (already defined in `configurator.css` since v17, just not previously used on `#cadence`'s cards). The bullets are unchanged real `.tier-card ul li` checkmarks; only the bold day-prefixes came out, since dropping them was what actually fixed the "3 uniform Agenda Anchors" ask (a bulleted list with one bolded run-in label per line and two without is what reads as inconsistent, not a wrapping bug in the CSS itself). The bottom-alignment fix, so a divider and its CTA button sit at the same height across all three cards regardless of how much mandate/bullet text sits above, reuses `.card-bottom-lock` verbatim, the same wrapper v22 built for the partner cards' footers, now wrapping `.tier-deliverable` + `.tier-actions` together here too. The brief's own proposal was a full parallel `.cadence-grid`/`.cadence-card`/`.cadence-header`/`.deliverable-block` component system with its own CSS file addition; none of it shipped, since the real `.tier-grid`/`.tier-card`/`.tier-deliverable`/`.card-bottom-lock` system this section already runs on (shared with `#formats` and `#engagement-models`) covers the same ground.

The brief's own copy for the mandate lines, the 3 standardized bullets, and the deliverable text was used close to verbatim (tightened slightly for house style, no content changes), since it already achieved the parallel structure being asked for.

**The brief's nav-sync request was applied as given, and it's worth a second look.** It asked to repoint the "Ways to Work" nav link, currently the only nav entry for `#engagement-models`, to `#cadence` instead, relabeled "Cadence", so the label matches "Choose Your Cadence". Done on both pages. The side effect: `#engagement-models` ("Three Ways to Work With Us") now has no nav entry pointing at it at all, the same gap `#cadence` used to have before this change, just moved to a different section. This wasn't flagged as optional in the brief, it came with exact markup, so it shipped, but it's a real trade-off (one section's reachability for another's), not just a label tweak, and worth confirming was the intent rather than an incidental rewrite.

Verified: tag-balance check on both pages (clean), `configurator.css` brace balance (clean, unchanged), `node -c app.js` (unchanged), a jsdom check confirming all three cards carry a mandate, exactly 3 bullets with no `<strong>` prefixes, and a `.card-bottom-lock` wrapping both the deliverable block and the CTA button, and a grep confirming "Ways to Work" no longer appears in either page's nav and `#engagement-models`'s own on-page heading ("Three Ways to Work With Us") is untouched.

## Formats summary cards polished: pills, single toggle, image bleed (v26)

The three `#formats` summary cards had three separate rough edges: the metric-pill row used `.client-badges`, a muted grey treatment built for logo strips, not tags; each card carried two competing CTAs, an "Inspect full scope" text toggle and a separate "Build this format" button, with no shared visual language between them; and the card image needed a per-card inline negative-margin hack to bleed to the card's edges, since `.tier-card` carries padding on all sides by default. All three are fixed this round without adding a new card shell.

The cards moved off `.tier-card` onto `.place-card`/`.place-image`/`.place-body`, the real component family `#places` already runs on. `.place-card` carries zero padding and `.place-image` sits flush at the top by construction, so the image now bleeds to the top and side borders natively, with no inline `margin`, `border-radius`, or `aspect-ratio` overrides left on any of the three cards. The metric pills swapped from `.client-badges` to `.lang-pill`, the real chip style `#collective`'s language-delivery row already uses, wrapped in one new line of CSS, `.pill-metrics-row{display:flex;flex-wrap:wrap;gap:6px;}`, since `.lang-pill` has no built-in wrapping container of its own. The footer, pills plus button, locks to the same baseline across all three cards using `.card-bottom-lock`, the same wrapper reused a third time now, after the partner cards in v22 and the cadence cards in v25.

The two competing buttons are gone, replaced by one. Each card now carries a single `.btn` that both opens that card's panel in the drawer below and shows, through its own fill and label, whether that panel is currently open: `.btn-secondary` with "Inspect Scope & Curriculum" when closed, `.btn-primary` with "Viewing Scope Below" when open. Both are the real, already-used button classes, no new visual states were defined for this. `app.js` swaps the class pair, the `aria-expanded` attribute, and the label together in one function.

This is a real trade-off worth a second look. The old "Build this format" button jumped straight to the configurator from the card itself, without opening the drawer first. That direct path is gone. Opening a card now always means reading its scope in the drawer first; reaching the configurator from `#formats` happens from the drawer's own "Configure this format" button, unchanged from v23, once a card is open. The brief asked explicitly to remove the competing dual buttons, and this is what shipped, but it does mean one click became two for anyone who already knew which format they wanted going in.

The dead `.format-inspect-toggle` and `.format-inspect-arrow` CSS rules from v23 came out of `configurator.css`, along with the matching class off the card markup, since the toggle button now swaps its whole label and icon via `innerHTML` rather than rotating a fixed arrow glyph.

Verified: tag-balance check on `index.html` (clean), `configurator.css` brace balance (clean), `node -c app.js`, an image-path-existence check (all 27 image references resolve, no new images added), and a jsdom test confirming each card carries exactly one toggle button, clicking a toggle correctly swaps `.btn-primary`/`.btn-secondary`, `aria-expanded`, and the visible label on all three cards, the matching drawer panel opens and the other two stay hidden, and `selectFormatAndScroll` and the drawer's own "Configure this format" buttons are unaffected.

## Formats summary cards: pills replaced with a checkmark micro-list (v27)

The metric-pill row under each `#formats` card's mandate line is gone. It had already changed treatments twice, `.client-badges` in v23 then `.lang-pill` in v26, and both were horizontal rows of short tags that could still wrap awkwardly mid-phrase inside a compact card, with three cards wrapping differently next to each other. It's replaced with a plain vertical 3-item checklist.

The list reuses the real, dormant `.outcomes-list` checkmark styling verbatim, the same component that already renders the two-column spec lists in the drawer directly below the grid, narrowed to one column with the existing `.outcomes-list-1col` modifier, itself already built in v23. No new list or checkmark style was invented: a card and its own open drawer panel now read with the same checkmark language instead of two different tag treatments. The only new rule is `.card-bottom-lock .outcomes-list`, which zeroes the list's own default browser margin and left indent, since `.outcomes-list` was built for the full-width drawer panel and carries spacing that doesn't belong inside a narrow card body.

`.pill-metrics-row` and the `.lang-pill` markup on these three cards come out entirely. `.lang-pill` itself is untouched and still live at `#collective`'s language row.

Copy for the three list items on each card is a tightened version of the mandate each card already carried, not new claims: co-founders keeps the horizon-labs, founder-stamina, and masia-venue points; C-suite keeps the friction-matrix, RACI, and 90-day-compact points; scale-up keeps the dual-track, turnkey-logistics, and catamaran points.

Verified: tag-balance check on `index.html` (clean, `partners.html` unaffected), `configurator.css` brace balance (clean), `node -c app.js` (unchanged), an image-path-existence check (all 27 image references resolve, no new images added), a grep confirming no remaining `.pill-metrics-row` or `.lang-pill`/`.client-badges` references inside `#formats`, and a jsdom test confirming each card carries exactly one 3-item checklist and one toggle button, and that clicking a toggle still opens the correct drawer panel with `selectFormatAndScroll` unaffected.

## Cadence detail drawer added, matched to the #formats interaction pattern (v28)

`#cadence` now works exactly like `#formats`: three compact cards up top, one full-width detail drawer below the grid holding a concrete day-by-day sample agenda for whichever cadence is open, and a single toggle button per card whose fill, label, and arrow are the open-closed indicator.

Nothing about the cards' own shell changed. They're still the real `.tier-card` system, unchanged since v3/v12/v25, since these cards never had the pill-wrapping problem `#formats` was fixing back in v26 and v27. The three bullets under each mandate were tightened to shorter phrases so the cards stay compact, still the same real checkmark-bullet styling `.tier-card ul li` has always used. The drawer itself is not a new component: it's the real `#formats` drawer, `.format-detail`, `.format-detail-head`, `.format-detail-columns`, `.case-docket-badge`, `.outcomes-list`, all built in v23, reused wholesale for a second section rather than rebuilt under cadence-specific names. A card and its own open agenda, and a format card and its own open scope panel, now read with one shared visual language instead of two unrelated ones.

The one new CSS rule is `.format-detail-columns-3col`, since the three-day agenda needs three day-columns side by side where the two-day and five-day agendas each need two. It's a sibling of the real `.outcomes-list-1col` modifier from v23, not a redesign of `.format-detail-columns` itself, and the drawer's existing mobile collapse rule already catches it too, since both classes land on the same element. The toggle button reuses the real `.btn`/`.btn-secondary`/`.btn-primary`/`.btn-sm` classes exactly as `#formats`' own v26 consolidated toggle does, swapping fill and label via `app.js`'s new `activateCadence()`, a direct mirror of `activateFormat()` a few lines above it. The three-day cadence opens by default, matching its "Most Effective" badge, the same way the C-suite format opens by default above it.

This is the same trade-off v26 made for "Build this format," now made a second time. Each card used to jump straight to `selectCadenceAndScroll()` with one click. That direct jump is gone: locking in a cadence now happens from the drawer's own "Select N-Day Cadence" button, in the same header slot `#formats`' "Configure this format" button sits in, once that cadence's agenda is open.

Verified: tag-balance check on both pages (clean), `configurator.css` brace balance (clean), `node -c app.js`, an image-path-existence check (all 27 image references resolve, no new images added), and a jsdom test confirming each of the three cadence cards carries exactly one toggle button, the three-day cadence opens by default with the correct drawer panel visible and the other two hidden, clicking a toggle correctly swaps `.btn-primary`/`.btn-secondary`, `aria-expanded`, and the label, the three-day panel carries three day-columns via `.format-detail-columns-3col` while the two-day and five-day panels carry two each, and that the `#formats` toggles and `#cadence` toggles operate independently without interfering with each other's state.

## Partners roster refresh, nav label, and real headshots (v29)

Three changes this round, all on `index.html` and `partners.html`: the nav label, real headshots into the specialist bench, and a swap in who's on that roster.

Nav label: "Partners & Bench" became just "Partners" in all four spots it lived, header nav and footer Engagements list on both pages. Plain text swap, no markup changed.

The bigger change is on `partners.html`'s "Specialist Practice Leads" grid (`#bench`). Since v19 that section used the real `.advisor-grid` / `.advisor-card` component with every card falling back to `.initials`, since no photos existed yet. Six real headshots arrived this round and all six cards now carry an actual `<img>` inside `.advisor-photo` instead of initials. `styles.css` already had `.advisor-photo img{width:100%; height:100%; object-fit:cover; filter:grayscale(1);}` sitting there unused since v19, so the photos needed zero new CSS, they just drop into the same 56px circular frame with the same grayscale treatment every other headshot on the site already gets. Checked first, as usual, before touching anything: the brief proposed a whole new parallel system, `.partners-roster-grid` / `.partner-card-sm`, targeted at `styles.css` directly. None of it shipped. `.advisor-card` already does everything that system was trying to do, and `styles.css` stays untouched.

Andrea Ross and Bree Aesie came off the roster entirely, cards and all. Colleen Tartow, PhD and Angus Nelson went in using the same `.advisor-card` markup as everyone else. Tiffany Missiha, Daniel Pascual, M&aacute;rcio Marcos, and Bryan Kramer keep their existing bios and badges, just with a real photo now instead of initials. Six cards in, six cards out, same 3-column grid.

Images landed at `assets/team/angus-nelson.png`, `assets/team/colleen-tartow.png`, `assets/team/marcio-marcos.png`, `assets/team/daniel-pascual.png`, `assets/team/bryan-kramer.png`, and `assets/team/tiffany.jpeg` (that last one saved with a `.jpeg` extension to match the file's actual JPEG format, following the same real-extension-over-assumed-extension fix v19 made for Luisa's headshot).

One open item flagged, not resolved: Colleen Tartow and Angus Nelson are brand new to this roster and nobody supplied their domain, bio, or client badges. Rather than invent professional credentials for two real named people, both cards ship with the photo and name real and everything else marked plainly as placeholder text, "Domain to be confirmed" and a bio line saying it's pending. Needs Sven's actual copy before this goes live.

Verified: tag-balance check on both pages (clean), `node -c app.js` (untouched this round but re-checked anyway), `configurator.css` brace balance (untouched, still clean), an image-path-existence check confirming all six new headshot files resolve, and a grep confirming no leftover references to Andrea Ross, Bree Aesie, or the old "Partners & Bench" label remain anywhere in either file.

Files touched this round: `index.html` (nav label, four spots), `partners.html` (nav label, footer label, the six-card bench grid), `assets/team/` (six new headshot files). `styles.css`, `configurator.css`, `app.js` untouched.

## Cadence drawer agendas rebuilt around Day 0 arrival and an 18:00 departure anchor (v30)

Sven asked for five things across the three `#cadence` drawer agendas: an explicit "Day 0" arrival evening on all three, so Day 1 opens fresh at 08:30/09:00 instead of mid-travel; a firm 18:00 departure anchor on the last working day of each; the 3-day agenda upgraded to three full operational days plus a dedicated half-day Catalan immersion; the 5-day agenda restructured into three phases, Strategy (Days 1-2), Leadership & Immersion (Day 3), Execution & Roadmaps (Days 4-5); and clean, responsive timeline typography matching the site.

The brief proposed a full parallel system for this, `.agenda-timeline-grid`/`.agenda-day-block`/`.agenda-schedule`/`.cadence-meta-badge`/`.agenda-subhead-pill`, with its CSS aimed at `styles.css` directly. None of it shipped. `styles.css` never gets edited, brief or no brief, and the real drawer shell this page already runs on, `.format-detail-head`/`.format-detail-columns`/`.format-detail-columns-3col`/`.format-detail-col`/`.outcomes-list`, covers the same ground the brief's new system was reaching for.

The new arrival/duration/departure summary line under each panel's `h3` ("Arrival Day 0 &middot; 3 Full Operational Days &middot; Departure Day 3 at 18:00") reuses `.lang-pill` verbatim, the real chip built in v8 for `#collective`'s language row and already reused twice since. The brief's own proposed CSS for its `.cadence-meta-badge`, 3px 8px padding, 2px radius, cream fill, hairline border, is functionally identical to `.lang-pill` already sitting in the file, so nothing new was needed there. The "Day 0" sub-label that sits above Day 1's own heading in the 3-day and 5-day panels (where the arrival evening shares a column with the first full day rather than getting one of its own) reuses `.case-docket-badge` a second time within the same panel, in place of the brief's `.agenda-subhead-pill`.

The one new rule is `.format-detail-col-boxed`, a cream background, hairline border, 2px radius box wrapping each day's heading and list. It matches this build's house corner radius rather than the 16px the brief borrowed from `.case-docket-outcome`, a real dormant component that turned out to be the wrong shape for this: `.case-docket-outcome` is built for one big centered stat, not a dense multi-item schedule list, so its heavy 40px/36px padding and rounded card treatment weren't reused even though the cream-box idea itself came from checking that component first. The box became necessary once the 2-day panel's first column and both outer columns of the restructured 5-day panel started stacking two distinct day-blocks each, arrival evening plus a full day, in v30's case, since an unboxed pair of headings and lists back to back read as one undifferentiated wall of text rather than two days.

The brief's own list-bullet CSS used an em dash (`content:"&#8212;"`) as the glyph. That didn't ship, on two counts: Sven's standing rule against the em-dash character anywhere in this build, prose or CSS-generated content alike, and because every other list in this drawer already speaks the real `.outcomes-list` checkmark language, so a second bullet style would have been a new inconsistency, not a fix.

Content changes, not just structure: the 2-day agenda gained a Day 0 arrival-evening block (private check-in, a low-key welcome dinner) ahead of Day 1, and Day 2 now explicitly closes at 18:00 instead of 16:30. The 3-day agenda's Day 2 now carries the half-day Catalan immersion, a private transfer to Port Vell for a three-hour chartered catamaran along the coast, in the early afternoon rather than folding the coastal sail into Day 3 as before, and Day 3 itself is now a full operational day (synthesis, pod alignment, ratification) closing at 18:00 rather than winding down informally at sunset. The 5-day agenda moved from two columns to three, matching the new three-phase structure: Days 1-2 keep the urban kickoff and horizon-labs content but are now explicitly framed as "Strategy," Day 3 becomes its own "Leadership & Immersion" column with a half-day guided sail or Montserrat hike, and Days 4-5 close out as "Execution & Roadmaps," ending with an explicit 18:00 departure that the previous version didn't state. All new copy stays grounded in venues and vocabulary already established elsewhere on the page, Girona estate, Pened&egrave;s, Port Vell, Poblenou/Norrsken, Montserrat, the same standing caution from v28 still applies: it's invented sample scheduling, not confirmed operational fact, and still wants a pass against what Sven's team actually runs before this goes live.

Verified: tag-balance check on `index.html` (clean, `partners.html` unaffected and confirmed unchanged), `configurator.css` brace balance (clean), `node -c app.js` (unchanged), an image-path-existence check (unaffected, all resolve), a grep confirming no em-dash character anywhere in the new drawer content or the new CSS block, and a jsdom test confirming all three toggle buttons and panels still exist, each panel now carries exactly three `.format-detail-col-boxed` blocks, clicking a toggle still correctly swaps which panel is hidden and which toggle button is primary, the 3-day panel's text contains both the 18:00 departure line and the half-day immersion line, and the 5-day panel's grid now carries the `.format-detail-columns-3col` class with three columns instead of two.

Files touched this round: `index.html` (`#cadence` drawer's three panels), `configurator.css` (`.format-detail-col-boxed`, `.cadence-meta`), `README.md`. `styles.css`, `app.js`, `partners.html`, every other section, every asset untouched.

## The experience gallery (#experience-gallery)

The "Inside the Barcelona Experience" cinema filmstrip sits between "Three Places to Go" and "Three Ways to Work With Us." It's two full-bleed horizontal photo tracks (`.filmstrip-track-left` / `.filmstrip-track-right`) that auto-scroll in opposite directions, pause on hover, and fade at both edges — the scroll-loop and gradient-mask technique is the same one already powering the real stylesheet's proof marquee (`.marquee`/`.marquee-track` in `styles.css`), just re-scaled from text to photography and split into two directions instead of one. Each track holds 4 unique frames plus the same 4 duplicated once more (`aria-hidden="true"`) so the loop has no visible seam.

**Flagging the photo sourcing, since it matters here:** the brief asked for 8 very specific scenes (a plenary debate, an outdoor executive work session, the Poblenou loft, the historic masia, an open-hearth dinner, a catamaran sunset sail, a vineyard at dusk, and a compact-signing moment), but only 2 of the 10 photos sent for this round actually depicted their intended scene:

- `04-masia-courtyard.jpeg` — the aerial estate-with-vineyard-hills photo. A real, strong match.
- `03-urban-atelier.jpeg` — the industrial loft interior. A real, strong match (confirmed as a different photo from the loft already used in the Places section, not a duplicate).

The other 6 slots were filled with the closest available stand-ins from generic Barcelona tourism photography, per your call to proceed best-effort rather than wait:

- `01-plenary-debate.jpeg` — a ceramics-workshop group photo (the full cohort gathered indoors; no actual debate).
- `02-mountain-terrace.jpeg` — a hiking-trail group shot from behind (outdoors in nature; not a working session).
- `05-open-hearth.jpeg` — a rooftop restaurant terrace, set tables (elegant dining; no open hearth).
- `06-catamaran-sunset.jpeg` — the W Hotel Barcelona beachfront (coastal/sea-facing; no catamaran).
- `07-vineyard-dusk.jpeg` — the Montserrat monastery mountains (elevated Catalan landscape; not a vineyard, not dusk).
- `08-compact-delivery.jpeg` — the Camp Nou stadium interior (a large formal venue; no signing moment). This is the weakest match of the eight.

Two of the ten photos (a Sagrada Família aerial and a Barcelona beach aerial) weren't used at all — nothing in the brief pointed to where they'd go. Given the brand brief explicitly avoids generic stock-photo imagery, I'd treat this section as a placeholder pass rather than final: the next real photography delivery should prioritize the 6 loose-match slots above, especially the compact-signing frame.

**Caption/image mismatch fixed, and files renamed to match (v15).** The filmstrip's tags and captions were rewritten to describe what each photo actually shows, rather than the generic scene the original slot names implied. Looked at every one of the 8 frames directly before touching anything, and found 5 clean matches to the update brief's own descriptions: `04-masia-courtyard.jpeg` (aerial masia estate) &rarr; "The Masia Sanctuary," `07-vineyard-dusk.jpeg` (actually the Montserrat monastery, not a vineyard) &rarr; "The Montserrat Foothills," `02-mountain-terrace.jpeg` (actually a forest-trail hiking shot, not a terrace session) &rarr; "The Trail Strategy," `06-catamaran-sunset.jpeg` (actually the W Hotel and Barceloneta beach, not a catamaran) &rarr; "The Mediterranean Coastline," and `08-compact-delivery.jpeg` (Camp Nou stadium interior) &rarr; "The Iconic Stadium." The brief's sixth slot, a kitchen/open-fire cooking photo, has no match anywhere in the current asset set: `01-plenary-debate.jpeg` is a ceramics workshop (tactile, but no fire or food) and `05-open-hearth.jpeg` is a rooftop dining terrace (no hearth, no fire), so rather than force either one under a "Culinary Hearth" label that doesn't describe what's in the frame, both got their own honest captions instead ("The Maker's Table" and "The Terrace Table"). If a real open-fire dining or cooking photo turns up later, that's the natural next slot to fill.

Since the whole bug was filenames and captions drifting apart in the first place, the 6 photos that got new captions were also renamed to match (`01-maker-workshop.jpeg`, `02-trail-strategy.jpeg`, `05-terrace-dining.jpeg`, `06-mediterranean-coastline.jpeg`, `07-montserrat-foothills.jpeg`, `08-iconic-stadium.jpeg`), so the next person editing this file doesn't inherit the same mismatch. `03-urban-atelier.jpeg` and `04-masia-courtyard.jpeg` were already correctly named and untouched. All 16 `<img>` references (8 visible frames plus their 8 `aria-hidden` scroll-loop duplicates) were updated together and verified with a jsdom check that all 8 unique src paths resolve and none of the old filenames remain.

## The social anchors section (#immersions)

New "Calibrated Decompression. Zero Forced Fun." section, sitting directly below the filmstrip and above "Three Ways to Work With Us": three social/experiential anchors (an open-fire hearth and sommelier masterclass, a private catamaran debrief, and vineyard/Montserrat walking plenaries), framed explicitly against generic corporate team-building.

Checked `styles.css` first, as always, before writing anything. The brief's own spec came with a full hand-rolled `.immersions-grid`/`.immersion-card` CSS block (numbered index, tag, heading, paragraph, a meta-pill row pinned to the bottom), but that's structurally identical to `.tier-grid`/`.tier-card`, the exact component already powering the formats, cadence, and engagement-models sections on this same page, so it was reused again rather than adding a fourth near-duplicate card system. The "01 &middot; Tactile Connection" numbered index line reuses `.tier-label` exactly as the cadence cards already do; the body copy is a plain `<p>`, matching how the engagement-models cards handle theirs; and the bottom meta-pill row reuses `.client-badges` verbatim for its pill styling (the same monochrome tag treatment already on the operator cards), with one new small rule, `.immersion-meta`, added only to pin that row to the card's bottom edge with a top hairline, since nothing in the real stylesheet already does that specific bit. That's the only new CSS this section needed.

No "most popular" badge was added to the middle card, since the brief didn't call for one there, unlike the cadence and formats sections which do feature one option. Background is cream with a bottom hairline, matching the spec's own request, sitting between the dark `#experience-gallery` cinema section above and the (unstyled, cream-by-default) `#engagement-models` section below.

## The cadence section (#cadence)

New "Choose Your Cadence" section sitting between "Three Places to Go" and the experience gallery: three duration options (2, 3, and 5 days), each with an operational rhythm, a "Key Monday Deliverable" callout, and a CTA that scrolls to the configurator.

Checked styles.css first, as always, and this one needed no new CSS at all: `.tier-grid`/`.tier-card`/`.tier-card.tier-featured`/`.tier-badge-pill`/`.tier-label`/`.tier-subhead`/`.tier-deliverable`/`.tier-deliverable-label`/`.tier-actions` are the exact same component already powering both the formats section and engagement-models section, plus a "Key Monday Deliverable" callout pattern (`.tier-deliverable`) that was itself sitting dormant, unused anywhere on the build until now, and is a near-exact fit for the brief's own "Key Monday Deliverable" block. The original request's oversized duration numeral and its own hand-rolled `.cadence-*` CSS block were dropped in favor of this real, already-shipped card pattern (the same overline-kicker-plus-heading hierarchy `.tier-label`/`h3` already use for the numbered formats), the same call made for the operators section in v8 and the FAQ in v11.

`window.selectCadenceAndScroll()` was added to `app.js` alongside `selectFormatAndScroll`. Unlike the format cards, there's no duration/cadence step in the 5-step configurator to pre-select, so it's a plain scroll to `#configurator`, matching what was actually requested; the `cadenceKey` argument is accepted but unused, left in case a real duration step gets added to the configurator later. Verified with a jsdom check that the function exists on `window` and calls `scrollIntoView` on click.

The pasted spec again carried broken citation-marker artifacts through the copy, stripped out before anything was written, and its one em dash was rewritten to match Sven's standing no-em-dash preference.

## The FAQ section (#faq)

New accordion section sitting between "Three Ways to Work With Us" and the configurator, answering the operational questions a Chief of Staff or People lead tends to raise before submitting a brief: planning overhead, lead times, the 90-day compact, how leadership tension is handled, pricing structure, languages, and internal co-facilitation.

Checked `styles.css` first, per the standing habit, and this is the strongest dormant-component match found on the whole build: a complete `#faq` FAQ system already existed there, unused, matching the request almost exactly, including a two-column layout with a sticky intro (eyebrow, heading, sub-copy, a "talk to us directly" bridge link) beside the accordion itself, JS-toggled `.faq-item.open` state, and a `grid-template-rows` transition for the expand/collapse (not native `<details>`/`<summary>`, which the original request specified but the real stylesheet doesn't use anywhere). Used the real component as-is rather than the requested native-accordion markup and a new hand-rolled CSS block, for the same reason as the v8 operators rebuild: it's the site's own real, on-brand pattern, not an approximation of one. No new CSS was needed in `configurator.css` for this section at all.

Because the real component drives its open/closed state with a class rather than native disclosure behavior, `app.js` needed a small accordion handler: click a `.faq-question`, it opens that item and closes whichever other item was open (one open at a time), toggling `aria-expanded` alongside the `open` class. Verified end-to-end with a jsdom smoke test: clicking an item opens it, clicking a second item closes the first and opens the second, clicking the open item again closes it.

The five languages listed (English, German, French, Portuguese, Spanish) match the language pills already on Sven's own operator card in `#collective`, not the four in the original request draft, since the real page already commits to five. The direct-contact link in the pricing/contact answer and the sidebar bridge link both point at `contact@kuma.partners`, the same address already used in the footer, rather than the sales-y "book a 20-minute call" footer callout in the original request, since Kuma's positioning avoids that kind of direct-CTA styling elsewhere on the page.

Not done this round, flagged instead: no nav link was added for `#faq`. The nav has only been changed in past rounds when Sven specified the exact link set (v5), so this was left for him to call rather than guessed at.

**Expanded to ten questions (v13).** Three new items added, keeping the same real `.faq-item`/`.faq-question`/`.faq-answer` markup rather than the native `<details>`/`<summary>` the update spec used: larger cohorts up to 100 people (after the lead-time question), a mutual NDA question (after leadership tension), and rescheduling/date flexibility (right after pricing). The pricing answer's upper band moved from &euro;60,000 to &euro;150,000, and the lead capture form's Budget dropdown was split to match: `&euro;60,000 &ndash; &euro;100,000` and a new `&euro;100,000 &ndash; &euro;150,000+` band, updated in both `app.js`'s `renderLeadForm()` and the hidden static Netlify-detection form in `index.html` together, since the field's option values have to match verbatim for Netlify Forms to accept either version of the submitted value. Verified with a jsdom check that all ten items render in the right order and the accordion still opens one at a time.

## The operators section (#collective)

This section was rebuilt around card components that already existed in the real stylesheet, unused anywhere on this build until now, the same pattern as `.problem-stat` and `.marquee` elsewhere on this page: `.partner-card-lg`, `.partner-photo` (56px circular, grayscale filter baked into the class), `.track-record`, `.client-badges`, `.social-row`, and `.team-cta` all come straight from `styles.css`, since they're literally the same components the real partners.html team page uses. The brief's own HTML spec called for inline overrides (72px, full color photos), but since matching partners.html's actual visual hierarchy was the explicit goal, the real dormant component was used as-is instead of the override, on the view that the real component is a closer match to partners.html than a guessed-at inline style would be. Worth a look if a different photo treatment was actually intended.

The only genuinely new pieces, added to `configurator.css`: the language-delivery pills (`.lang-pill`/`.operator-languages`, nothing like this existed in the real stylesheet) and a small paragraph-size rule for the bio text, matching the sizing of the sibling `.advisor-bio` pattern. The brief's spec asked for the new CSS to go into `styles.css`; it went into `configurator.css` instead, consistent with every prior round on this build (`styles.css` stays the untouched real stylesheet).

Photos: `assets/team/dr-sven-mulfinger.jpeg` (cropped square from the supplied portrait, framed on the face) and `assets/team/farid.jpeg` (used as supplied, already square). Both render through `.partner-photo img`'s existing grayscale filter, so the source files themselves didn't need to be desaturated.

Two placeholder details worth flagging before this goes live: Farid's LinkedIn link points at `https://www.linkedin.com` (no profile slug was given) and the "Explore Full Partner Bench" button points at `https://www.kuma.partners/partners`, both exactly as specified in the brief. Confirm the real destinations before pushing.

**Expanded to a third operator, Luisa Ábalo (v14).** The section went from a 2-card layout to 3: eyebrow, headline, and subhead rewritten around "three distinct superpowers," a small `.superpower-badge` tag added above each name (Sven: Executive Mediation, Farid: Venture Innovation, Luisa: Organizational Scaling), and a new "Dual-Facilitator Architecture for Cohorts Over 20 Leaders" banner between the grid and the bench-extension callout.

Going from two cards to three needed no new grid CSS: `.partner-grid`'s own base rule in `styles.css` is already a 3-column grid, and `.partner-grid-2col` was only ever a modifier narrowing it to two, so the whole layout change was dropping that one class. The only genuinely new CSS is `.superpower-badge` (a small tag, nothing in the real stylesheet does this compact a label) and `.standard-banner` for the dual-facilitator notice, since neither `.problem-callout` (single-line, card-scoped) nor `.offsite-specs-note` (a footer line inside an existing box) fits a standalone label-plus-heading-plus-paragraph banner. Both went into `configurator.css`.

**No photo was provided for Luisa**, and none was generated for her. Her card uses `.partner-photo .initials`, a real fallback treatment already built into the site's own CSS (a plain "LA" in the same circular frame the other two photos sit in) rather than either a broken image or an AI-rendered likeness of a real, named person, which isn't something to fabricate on spec. Swap in a real headshot at `assets/team/luisa-abalo.jpeg` whenever one's available and the `<img>` markup from Sven's or Farid's card drops in directly.

**Luisa's photo supplied (v15).** A real portrait was provided and is now live: cropped to a 480&times;480 square (matching the other two team photos' dimensions) and saved at `assets/team/luisa-abalo.jpeg`, with the `.initials` fallback swapped out for an `<img>` using the same alt-text pattern as Sven's and Farid's cards. It renders through the existing `.partner-photo img` grayscale filter automatically, same as the other two.

**Also unverified, ship-as-specified:** Luisa's LinkedIn URL (`linkedin.com/in/luisaabaloperez`) and `luisa@kuma.partners` came straight from the brief with no way to confirm them from here, same treatment as Farid's placeholder link above. The brief's `href` values for all three LinkedIn links arrived wrapped in markdown link syntax (`[url](url)`), a paste artifact; cleaned to plain URLs before going into the `href` attributes, since the markdown syntax would have broken the links as written.

## SEO, AI crawler access & structured data (v16)

Three files changed together for this round: `robots.txt`, `sitemap.xml` (new), and `<head>` in `index.html`.

**`robots.txt`** now explicitly allows the major AI-search and LLM-discovery crawlers alongside the general `User-agent: *` rule: `GPTBot`, `ClaudeBot`, `PerplexityBot`, `Google-Extended`, and `Applebot-Extended`. Functionally redundant with the wildcard allow already there (a bot that respects `*: Allow: /` doesn't need its own block to be allowed), but explicit per-bot rules are what these crawlers' own documentation recommends checking for, and it makes the intent legible to a human skimming the file rather than relying on the wildcard alone.

**`sitemap.xml`** is new, referenced from `robots.txt`'s `Sitemap:` line (which already pointed here before the file existed, an open item from earlier rounds). Single URL, since this is a one-page site: `https://offsite.kuma.partners/`, weekly changefreq, priority 1.0.

**`<head>` in `index.html`:** the `<title>` and `<meta name="description">` were rewritten from the original build's copy to a version more aligned with the H1/subhead as they exist after the v10 hero refactor. Added: `<link rel="canonical">`, an explicit `<meta name="robots">` (allowing full snippets and large image previews, since nothing on this page needs to be excerpt-limited), Open Graph tags (`og:type`/`og:url`/`og:title`/`og:description`/`og:image`), and a Twitter Card block. The OG/Twitter share image points at `assets/environments/masia-barcelona.jpeg`, the same photo already used in the places section, since nothing else on the page reads as a single representative "this is what it is" image the way that one does.

**JSON-LD (`@graph` of `ProfessionalService` + `FAQPage`):** the `FAQPage` entity's 10 questions are pulled verbatim from the real, current `#faq` accordion already on the page, not the four-question illustrative sample in the update brief. The brief's four only cover a subset (planning overhead, pricing, languages, larger cohorts) and don't match the accordion's actual 10 questions word-for-word; shipping a schema block that cites answers the visible page doesn't give, or omits six the page does give, would be exactly the kind of structured-data/visible-content mismatch Google's own guidelines warn against, so all 10 went in, same order as the page, plain-text `€`/`–` characters instead of HTML entities since JSON-LD strings aren't HTML-rendered. `ProfessionalService.priceRange` (`€15,000 - €150,000+`) matches the FAQ's own pricing answer, so the two don't contradict each other.

**Unverified, shipped as specified:** `nonameyet.com` as Nonameyet's own `url` in the `provider` array is not a domain confirmed to exist or belong to Nonameyet from here, same treatment as Farid's placeholder LinkedIn URL and the kuma.partners/partners bench-callout link in earlier rounds. Confirm it resolves (or drop the `url` field entirely) before push.

The brief's pasted spec wrapped nearly every URL in markdown link syntax (`[url](url)`) throughout `robots.txt`, `sitemap.xml`, the meta tags, and the JSON-LD string values, the same paste artifact seen in v14's LinkedIn hrefs. All of it was stripped to plain URLs before writing, since markdown syntax inside a `Sitemap:` line, an `xmlns` attribute, a `content` attribute, or a JSON string would have been literally invalid in every one of those contexts, not just cosmetically wrong.

Verified: the JSON-LD block parses with a JSON parser and reports 2 `@graph` entries (`ProfessionalService`, `FAQPage` with 10 `mainEntity` items), `sitemap.xml` parses as well-formed XML, no markdown-link artifacts remain anywhere in the three files, and a jsdom load confirms the new `<title>`/canonical/OG tags render and the `#faq` accordion still shows all 10 items.

## Partners & specialist bench page (v19)

New `partners.html`, linked from the "Backed by Kuma's Senior Advisor Network" callout in `#collective` (`.team-cta`'s href, previously pointing off-domain to `www.kuma.partners/partners`, now `/partners.html`), from the main nav (`.nav-links`, a new "Partners & Bench" item after "Ways to Work"), and from the footer's Engagements list. All three link updates keep the existing real components as-is: no new nav or footer markup, just a new `<li>` in each.

The brief's own HTML for the new page and its header/footer used a hand-rolled component set (`.site-header`, `.main-nav`, `.brand-logo`, `.footer-col`, `.footer-heading`, `.footer-links`, `.bench-grid`, `.bench-card`, `.bench-discipline`, `.bench-badges`, `.partner-grid-3col`, `.partner-card-wide`, `.manifesto-section`) that doesn't exist anywhere in the real stylesheet. None of it shipped. `partners.html` reuses index.html's actual header and footer verbatim (`header.site` / `.nav-links` / `.logo-jv`, `footer.site` / `.footer-grid` / `.footer-hubs`), with section-anchor hrefs prefixed `/` since they now point back at the homepage from a different page, and `aria-current="page"` on the Partners & Bench link rather than an invented "active" class.

The "Principal Partners" section on the new page is the same `.partner-grid` / `.partner-card-lg` component already live in `#collective`, with the same three cards (Sven, Farid, Luisa) carried over verbatim rather than rewritten, so the two pages never say something different about the same person. A fourth card for Vincent Az&eacute; was added in the same grid, using the real `.initials` fallback for his photo since none was supplied (same treatment as Luisa's card before her headshot arrived in v15) and only the fields the brief actually gave him (no track-record chips, languages, or social links were supplied, so none were invented).

The "Specialist Practice Leads" section (six bench specialists) uses `.advisor-grid` / `.advisor-card`, a complete card component already in the real stylesheet (photo-or-initials, name, a `.domain` label, an `.advisor-bio` paragraph, an optional `.social-row`) that had never been used anywhere on this build, discovered while checking styles.css first as usual. Its own comment in the source even calls the sibling `.partner-card-lg` "normalized to bench-card architecture," which is as strong a signal as this build has had that a real component was built for exactly this. The badge pills reuse `.client-badges` rather than the brief's invented `.bench-badges`. None of the six people had a supplied photo, so all six use the `.initials` fallback. The grid's 2-column default is overridden to 3 columns with an inline style on the wrapping div, the same one-off-sizing convention used elsewhere on this build (v10's hero metrics strip, the various section subheads), rather than a new CSS class.

The "manifesto" section (dark navy, "AI Cannot Solve In-Room Human Friction") reuses `.moat-section`, a real dark full-bleed section (eyebrow/h2/p on navy) that's existed in the stylesheet since before this build started but had never been used, instead of the brief's invented `.manifesto-section`. The one genuinely new rule is `.moat-pull-quote`, a small left-border italic treatment for the closing quote, since nothing dormant does a pull-quote and this was worth a few lines rather than another inline-style block.

The final CTA section drops the brief's `.btn-lg` modifier, which doesn't exist anywhere in the real stylesheet (only `.btn` and `.btn-sm` do), and uses the plain `.btn`/`.btn-primary`/`.btn-secondary` classes at their default size, matching every other CTA on the site.

Two corrections against the brief: Luisa's photo path was given as `assets/team/luisa-abalo.png`, but the real file (added in v15) is `assets/team/luisa-abalo.jpeg`; fixed to the real extension rather than shipping a broken image reference. The brief's `<head>` also included `<link rel="preconnect">` and a separate Google Fonts `<link>` tag, but this site already loads its fonts via an `@import` inside `styles.css` itself, so a second font load was dropped rather than duplicating it.

`sitemap.xml` gained a second `<url>` entry for `/partners.html` at priority 0.8, exactly as specified.

Verified: tag-balance check on both `index.html` and `partners.html` across the full tag set including `nav`/`header`/`footer`/`main`/`svg`/`path` (clean on both), a brace-balance check on `configurator.css` (clean), `node -c app.js`, both pages' JSON-LD blocks parse, `sitemap.xml` parses as well-formed XML, every image path referenced by either page resolves to a real file, and a jsdom load of both pages confirms `app.js` runs without throwing on `partners.html` (which has no `#configurator-app`, the element the configurator code guards on and returns early without) and that the mobile nav toggle still works on both pages.

Files touched this round: `partners.html` (new), `index.html` (`.nav-links`, `.team-cta` href, footer Engagements list), `configurator.css` (`.moat-pull-quote` appended), `sitemap.xml` (second `<url>`), `README.md`. `styles.css`, `app.js`, every other section, every other asset untouched.

## ProfessionalService schema warnings cleared (v18)

The Schema.org validator flagged two warnings on the `ProfessionalService` entity in the JSON-LD `@graph`: no `image` and no `telephone`. Both added directly to that entity. `image` reuses `assets/environments/masia-barcelona.jpeg`, the same photo already doing OG/Twitter share-image duty for this page, since it's already the one representative "this is what it is" shot on the site.

**`telephone` is a placeholder, not a real number.** `+34930000000` was supplied in the brief and shipped as given, but it reads as a filled-in template value (all zeros after the Barcelona area code) rather than Kuma's actual line. Confirm the real number before push, same unverified-detail treatment as `nonameyet.com` in v16 and Farid's placeholder LinkedIn since v8. Shipping a fabricated phone number in production structured data is worth catching before it goes live, since it's the kind of detail a search engine or a caller would actually try to use.

The brief's JSON snippet wrapped `@id`, `url`, `image`, and both `provider` URLs in markdown link syntax (`[url](url)`), the same paste artifact as v16; stripped to plain URLs before writing, since JSON string values don't parse markdown.

Verified: the full JSON-LD block still parses cleanly with a JSON parser (2 `@graph` entries), the `ProfessionalService` entity now carries both `image` and `telephone`, and the index.html tag-balance check stays clean.

Files touched this round: `index.html` (`<head>` JSON-LD block only). Everything else untouched.

## Calibrated Formats refactor (v17)

The `#formats` section was reframed from a "who will join us" list to a cohort-and-mandate structure: each card now names a room size (`2-6 co-founders`, `5-14 C-suite`, `15-100+ cross-functional`) and a one-line operational mandate, ahead of its feature list. Format 01 repositions around horizon-scanning and founder stamina against 3-5 year disruption instead of a generic reset pitch. Formats 02 and 03 now read as a 3-tier organizational ladder (founder pair up through full company), rather than three unrelated offerings.

The update brief proposed a new hand-rolled component system for this (`.formats-grid`, `.format-card`, `.format-image-wrapper`, `.format-num-tag`, `.format-feature-list`, and so on) with matching CSS for `styles.css`. None of that shipped. The section already ran on `.tier-grid`/`.tier-card`/`.tier-badge-pill`/`.tier-label`/`.tier-subhead`/`.place-image`/`.tier-actions`, the same component system `#cadence` and `#engagement-models` use, so the refactor rebuilt the copy and structure inside those existing classes instead of introducing a parallel one. `styles.css` was not touched, same rule as every prior round: it stays the real, unmodified kuma.partners stylesheet, and any new CSS goes in `configurator.css`. The only new rule needed was `.tier-mandate`, for the one-line italic mandate summary under each card's subhead.

The brief's feature-list bullets used a hand-drawn em dash (`content: "-";`) as the bullet glyph. Shipped instead with the real `.tier-card ul li::before{content:"✓"}` checkmark already in `styles.css` and already used by every other tier-card on the page, both to avoid introducing an em dash (Sven's standing no-em-dash rule applies to CSS-generated content, not just prose) and to avoid a second bullet treatment competing with the site's existing one. The brief's subhead copy also contained one literal em dash in running text ("required-from"), rewritten to a colon.

The brief's proposed `window.selectFormatAndScroll` rewrite for `app.js` was a functional regression: it dropped the `state.stepIndex = 1` jump and the `els.blueprintWrap`/`els.stepsWrap` visibility toggling that make the button jump straight to Step 2 with the archetype pre-selected (verified behavior since v9), leaving only a plain scroll-to-configurator. Shipped instead: the same verified jump-and-preselect behavior, simplified only to accept the archetype id directly instead of going through the now-removed `FORMAT_ARCHETYPE_MAP` translation table, since the new markup already calls it with `'cofounders'`/`'csuite'`/`'scaleup'`.

Verified: tag balance across the standard tag set, `configurator.css` brace balance, `node -c app.js`, and a jsdom load that clicks each of the three "Build this format" buttons and confirms it jumps to Step 2 with the corresponding archetype pre-selected (checked by simulating a Back-button click and reading which Step 1 option carries the `selected` class), plus that an unrecognized archetype id scrolls without throwing.

## How the blueprint logic works

Each of the 5 configurator steps stores one selection (`archetype`, `tension`, `setting`, `hospitality`, `facilitation`) in `app.js`. After step 5, it compiles a title, four pill tags, and four spec rows from two lookup tables: `TITLE_MATRIX` (keyed by `[tension][setting]`, 12 hand-written title combinations) and `DELIVERABLE_MATRIX` (keyed by tension, 4 deliverable phrases). Edit those tables directly to change the blueprint copy, nothing else needs to change.

The three "Build this format" buttons in the formats section (`onclick="selectFormatAndScroll('cofounders' | 'csuite' | 'scaleup')"`) pre-select the matching Step 1 archetype and jump straight to Step 2. As of v17 the buttons pass the archetype id directly, the same ids `STEPS[0]`'s options already use, so `selectFormatAndScroll` just checks membership in `VALID_ARCHETYPES` near the top of `app.js` rather than translating through a separate lookup table.

## Lead capture

Posts to Netlify Forms natively by default (no third-party webhook, since the site is already on Netlify). There's now a fallback: `FORM_WEBHOOK_URL`, a single constant at the very top of `app.js`, left empty. If this site ever moves off Netlify (GitHub Pages, Vercel), set that constant to a form-accepting URL (Formspree, Make.com, Zapier, or similar) and the submit handler posts the same urlencoded body there instead, no other code changes needed. The form collects work email, full name, role/title, company name, target quarter, and approximate budget, all required. Submissions currently appear under **Site configuration → Forms** in the Netlify dashboard. Set up email notifications there under Forms → your form → Settings.

## A code review request that didn't match this file

A recent request asked to fix a specific bug in `renderLeadForm()`: an extra orphan `</div>` right after the Company Name field that would break the form's DOM structure, plus unescaped angle brackets in the budget dropdown and non-UTF-8/BOM/CRLF issues. Checked all of it directly against this file before touching anything: the div count in `renderLeadForm()` is balanced (12 opens, 12 closes), the budget dropdown's `< €15,000` option is already properly HTML-entity-escaped as `&lt;`, and the file is clean UTF-8 with Unix line endings and no BOM. None of those specific bugs exist here, so nothing was changed for them. What did get done from that same request: all 36 `var` declarations converted to `const` (this file has no reassigned bindings, so `let` wasn't needed anywhere), the format-card pre-selection described above, the `FORM_WEBHOOK_URL` fallback, and an empty `.nojekyll` file added at the repo root as a harmless precaution in case GitHub Pages is ever turned on for this repo (the live site deploys through Netlify, not GitHub Pages, so this isn't fixing an active problem, just guarding against Jekyll processing if that ever changes).

## One thing flagged, not resolved

Every "Nonameyet" in this build (header lockup, footer copyright, the second operator's firm name) is shipped exactly as specified in the brief. Flagging it here in case it's still a placeholder rather than the final co-brand name, since it now appears in several places and would need updating everywhere at once if it changes.

## Deploying

Already connected to Netlify via continuous deployment from `sven353/offsite.kuma.partners`, and offsite.kuma.partners is already pointed at it. To push manually:

1. Extract this zip locally.
2. On GitHub, open the repo and use **Add file → Upload files**, dragging in the loose contents of the extracted folder (not the folder itself), so `assets/` lands at the repo root alongside `index.html`.
3. Commit directly to `main`.
4. Netlify redeploys automatically, usually within a minute or two.
5. Once live, check **Site configuration → Forms** in Netlify to confirm the `offsite-blueprint` form picked up the new `role` and `budget` fields alongside the existing ones.
